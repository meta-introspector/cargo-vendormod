[package]
name = "git_scanner"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
walkdir = "2"
rayon = "1.8"
log = "0.4"
env_logger = "0.10"

[dev-dependencies]
