use anyhow :: Result ; use cargo_vendormod :: config :: Config ; use cargo_vendormod :: global_dep_graph :: { DependencyEdge , DependencyEdgeType , DependencyNode , GlobalDependencyGraph } ; use clap :: { Parser , Subcommand } ; use petgraph :: algo :: toposort as petgraph_toposort ; use petgraph :: graph :: { Graph , NodeIndex } ; use petgraph :: visit :: EdgeRef ; use std :: collections :: { HashMap , HashSet } ; use std :: fs ; use std :: path :: PathBuf ; use std :: process :: Command ; use cargo_vendormod :: global_dep_graph :: GlobalDependencyGraphBuilder ; use cargo_vendormod :: global_dep_graph :: DependencyEdgeType ; # [doc = " Merge multiple project graphs into one global graph."] # [doc = " Reads all projects_graphs/*/graph/graph.json files, deduplicates nodes/edges,"] # [doc = " runs topological sort, outputs build order + upgrade plan + bare repo list."] fn cmd_merge (args : & GraphArgs) -> Result < () > { let input_path = args . input_path . as_ref () . ok_or_else (| | anyhow :: anyhow ! ("Input path (directory of project graphs) required for merge command")) ? ; println ! ("=== Global Graph Merge ===") ; println ! ("Input: {}" , input_path . display ()) ; println ! ("Output: {}" , args . output_dir . display ()) ; let mut graph_files : Vec < PathBuf > = Vec :: new () ; let entries = match fs :: read_dir (input_path) { Ok (e) => e , Err (e) => anyhow :: bail ! ("Failed to read input dir {}: {}" , input_path . display () , e) , } ; for entry in entries . flatten () { let project_dir = entry . path () ; if ! project_dir . is_dir () { continue ; } let graph_file = project_dir . join ("graph") . join ("graph.json") ; if graph_file . exists () { graph_files . push (graph_file) ; } } if graph_files . is_empty () { anyhow :: bail ! ("No graph.json files found under {}" , input_path . display ()) ; } println ! ("Found {} project graph files" , graph_files . len ()) ; let mut merged_nodes : HashMap < String , DependencyNode > = HashMap :: new () ; let mut merged_edges : Vec < DependencyEdge > = Vec :: new () ; let mut edge_set : HashSet < (String , String) > = HashSet :: new () ; let mut project_versions : HashMap < String , Vec < (String , String) > > = HashMap :: new () ; let mut projects : Vec < String > = Vec :: new () ; let mut all_sources : HashSet < String > = HashSet :: new () ; for gf in & graph_files { let project_name = gf . parent () . unwrap () . parent () . unwrap () . file_name () . and_then (| n | n . to_str ()) . unwrap_or ("unknown") . to_string () ; projects . push (project_name . clone ()) ; let content = fs :: read_to_string (gf) . map_err (| e | anyhow :: anyhow ! ("Failed to read {}: {}" , gf . display () , e)) ? ; let graph : GlobalDependencyGraph = serde_json :: from_str (& content) . map_err (| e | anyhow :: anyhow ! ("Failed to parse {}: {}" , gf . display () , e)) ? ; for node in & graph . nodes { let entry = merged_nodes . entry (node . id . clone ()) . or_insert_with (| | node . clone ()) ; if node . is_workspace_member { entry . is_workspace_member = true ; } project_versions . entry (node . crate_name . clone ()) . or_default () . push ((project_name . clone () , node . version . clone ())) ; if ! node . source . is_empty () && node . source != "workspace" && node . source != "crates.io" && node . source != "unknown" { all_sources . insert (node . source . clone ()) ; } } for edge in & graph . edges { let key = (edge . from . clone () , edge . to . clone ()) ; if edge_set . insert (key) { merged_edges . push (edge . clone ()) ; } } } println ! ("Merged: {} nodes, {} edges, {} projects" , merged_nodes . len () , merged_edges . len () , projects . len ()) ; println ! ("Unique git sources for mirroring: {}" , all_sources . len ()) ; let mut pg = Graph :: < DependencyNode , DependencyEdgeType > :: new () ; let mut pg_map : HashMap < String , NodeIndex > = HashMap :: new () ; for (id , node) in & merged_nodes { let idx = pg . add_node (node . clone ()) ; pg_map . insert (id . clone () , idx) ; } for edge in & merged_edges { if let (Some (& from_idx) , Some (& to_idx)) = (pg_map . get (& edge . from) , pg_map . get (& edge . to)) { pg . add_edge (from_idx , to_idx , edge . edge_type . clone ()) ; } } let build_order = match petgraph_toposort (& pg , None) { Ok (sorted) => { let order : Vec < String > = sorted . iter () . filter_map (| idx | pg . node_weight (* idx)) . map (| n | n . crate_name . clone ()) . collect () ; println ! ("Topological sort succeeded: {} crates in order" , order . len ()) ; order } Err (cycle) => { let mut cycle_crates = Vec :: new () ; if let Some (node) = pg . node_weight (cycle . node_id ()) { cycle_crates . push (node . crate_name . clone ()) ; for edge in pg . edges_directed (cycle . node_id () , petgraph :: Direction :: Incoming) { if let Some (source) = pg . node_weight (edge . source ()) { cycle_crates . push (source . crate_name . clone ()) ; } } } eprintln ! ("Warning: Circular dependency detected: {:?}" , cycle_crates) ; let partial_order : Vec < String > = pg . node_weights () . map (| n | n . crate_name . clone ()) . collect () ; partial_order } } ; let mut upgrade_plan : Vec < serde_json :: Value > = Vec :: new () ; let mut upgrade_count = 0 ; for (crate_name , versions) in & project_versions { let unique_versions : HashSet < & str > = versions . iter () . map (| (_ , v) | v . as_str ()) . collect () ; let project_list : Vec < & str > = versions . iter () . map (| (p , _) | p . as_str ()) . collect () ; if unique_versions . len () > 1 { let oldest = unique_versions . iter () . min () . unwrap_or (& "") ; let newest = unique_versions . iter () . max () . unwrap_or (& "") ; upgrade_count += 1 ; upgrade_plan . push (serde_json :: json ! ({ "crate" : crate_name , "versions" : unique_versions . iter () . cloned () . collect ::< Vec < _ >> () , "oldest" : oldest , "newest" : newest , "used_by" : project_list , "action" : format ! ("upgrade from {} to {}" , oldest , newest) })) ; } } println ! ("Upgrade candidates (version mismatches): {}" , upgrade_count) ; let out_dir = & args . output_dir ; fs :: create_dir_all (out_dir) ? ; let global_graph_path = out_dir . join ("global_graph.json") ; let mut unique_crate_names : Vec < String > = Vec :: new () ; { let mut seen : HashSet < & str > = HashSet :: new () ; for node in merged_nodes . values () { if ! node . is_workspace_member && seen . insert (node . crate_name . as_str ()) { unique_crate_names . push (node . crate_name . clone ()) ; } } } let merged_graph = GlobalDependencyGraph { nodes : merged_nodes . into_values () . collect () , edges : merged_edges , features : Vec :: new () , toml_structures : Vec :: new () , strongly_connected_components : Vec :: new () , partitions : Vec :: new () , metrics : cargo_vendormod :: global_dep_graph :: GraphMetrics { node_count : 0 , edge_count : 0 , workspace_members : 0 , direct_dependencies : 0 , transitive_dependencies : 0 , feature_gated_dependencies : 0 , dev_dependencies : 0 , build_dependencies : 0 , strongly_connected_components : 0 , diameter : 0 , average_degree : 0.0 , } , workspace_path : PathBuf :: new () , publish_order : build_order . clone () , topological_order : build_order . clone () , external_dependency_order : Vec :: new () , } ; fs :: write (& global_graph_path , serde_json :: to_string_pretty (& merged_graph) ?) ? ; println ! ("Global graph: {}" , global_graph_path . display ()) ; let build_order_path = out_dir . join ("build_order.json") ; let build_order_data : Vec < serde_json :: Value > = build_order . iter () . enumerate () . map (| (i , name) | serde_json :: json ! ({ "rank" : i + 1 , "crate" : name })) . collect () ; fs :: write (& build_order_path , serde_json :: to_string_pretty (& serde_json :: json ! ({ "total_crates" : build_order_data . len () , "order" : build_order_data })) ?) ? ; println ! ("Build order: {} crates -> {}" , build_order . len () , build_order_path . display ()) ; let upgrade_path = out_dir . join ("upgrade_plan.json") ; fs :: write (& upgrade_path , serde_json :: to_string_pretty (& serde_json :: json ! ({ "total_candidates" : upgrade_count , "plans" : upgrade_plan })) ?) ? ; println ! ("Upgrade plan: {} candidates -> {}" , upgrade_count , upgrade_path . display ()) ; let mirrors_path = out_dir . join ("mirrors.json") ; let sources_sorted : Vec < String > = all_sources . into_iter () . collect () ; fs :: write (& mirrors_path , serde_json :: to_string_pretty (& serde_json :: json ! ({ "total_mirrors" : sources_sorted . len () , "repositories" : sources_sorted })) ?) ? ; println ! ("Bare repos to mirror: {} -> {}" , sources_sorted . len () , mirrors_path . display ()) ; let projects_path = out_dir . join ("projects.json") ; fs :: write (& projects_path , serde_json :: to_string_pretty (& serde_json :: json ! ({ "total_projects" : projects . len () , "projects" : projects })) ?) ? ; println ! ("Projects: {} -> {}" , projects . len () , projects_path . display ()) ; let bare_dir = out_dir . join ("bare_repos") ; fs :: create_dir_all (& bare_dir) ? ; let mut cargo_replace : HashMap < String , String > = HashMap :: new () ; let mut mirrors_created = 0u32 ; for source in & sources_sorted { let repo_name = source . split ('?') . next () . unwrap_or (source) . split ('#') . next () . unwrap_or (source) . rsplit ('/') . next () . unwrap_or ("unknown") . trim_end_matches (".git") . to_string () ; let bare_path = bare_dir . join (format ! ("{}.git" , repo_name)) ; if ! bare_path . exists () { let _status = Command :: new ("git") . args (["init" , "--bare" , & bare_path . to_string_lossy ()]) . output () ; mirrors_created += 1 ; } if source . contains ("github.com") || source . contains ("git://") || source . starts_with ("https://") { let canonical = source . split ('?') . next () . unwrap_or (source) . to_string () ; cargo_replace . insert (canonical , format ! ("file://{}" , bare_path . to_string_lossy ())) ; } } println ! ("Bare repos created: {} -> {}" , mirrors_created , bare_dir . display ()) ; let cargo_patch_path = out_dir . join ("cargo_mirror_config.toml") ; let mut cargo_config = String :: new () ; cargo_config . push_str ("# Cargo mirror config — replaces git sources with local bare repos\n") ; cargo_config . push_str ("# Place in project_root/.cargo/config.toml\n\n") ; for (original , local) in & cargo_replace { cargo_config . push_str (& format ! (r#"[source."{original}"]
git = "{local}"
replace-with = "local-mirror"

"# ,)) ; } if ! cargo_replace . is_empty () { cargo_config . push_str ("[source.\"local-mirror\"]\n") ; cargo_config . push_str (& format ! ("directory = \"{}\"\n" , bare_dir . display ())) ; } fs :: write (& cargo_patch_path , cargo_config) ? ; println ! ("Cargo mirror config: {}" , cargo_patch_path . display ()) ; let flakes_dir = out_dir . join ("flakes") ; fs :: create_dir_all (& flakes_dir) ? ; let mut flakes_created = 0u32 ; for crate_name in & unique_crate_names { let crate_flake_dir = flakes_dir . join (crate_name) ; fs :: create_dir_all (& crate_flake_dir) ? ; let flake_content = format ! (r#"# Auto-generated flake for crate: {name}
# Generated by cargo-vendormod graph merge
{{
  description = "{name} — dependency crate";

  inputs = {{
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
    rust-overlay.url = "github:oxalica/rust-overlay";
  }};

  outputs = {{ self, nixpkgs, flake-utils, rust-overlay }}:
    flake-utils.lib.eachDefaultSystem (system:
      let
        overlays = [ (import rust-overlay) ];
        pkgs = import nixpkgs {{ inherit system overlays; }};
        rustToolchain = pkgs.rust-bin.stable.latest.default;
      in
      {{
        packages.default = pkgs.rustPlatform.buildRustPackage {{
          pname = "{name}";
          version = "0.1.0";
          src = ./.;
          cargoLock.lockFile = ./Cargo.lock;
          nativeBuildInputs = with pkgs; [ rustToolchain pkg-config ];
        }};

        devShells.default = pkgs.mkShell {{
          buildInputs = with pkgs; [ rustToolchain ];
        }};
      }});
}}
"# , name = crate_name) ; fs :: write (crate_flake_dir . join ("flake.nix") , flake_content) ? ; flakes_created += 1 ; } println ! ("Crate flakes generated: {} -> {}" , flakes_created , flakes_dir . display ()) ; println ! ("\n=== Merge Complete ===") ; Ok (()) }