[package]
name = "fromstr"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: FromStr"

[dependencies]
format = { path = "../format" }
partitioningalgorithm = { path = "../partitioningalgorithm" }
