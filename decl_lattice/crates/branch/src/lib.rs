use anyhow :: { Context , Result } ; use std :: fs ; use std :: path :: { Path , PathBuf } ; use std :: process :: Command ; use toml_edit :: { DocumentMut , Value } ; fn get_submodule_branch (doc : & DocumentMut , section : & str) -> Option < String > { let key = format ! ("submodule \"{}\".branch" , section) ; doc . get (& key) . and_then (| v | v . as_str ()) . map (String :: from) }