use anyhow :: { Context , Result } ; use std :: path :: Path ; use std :: process :: Command ; use std :: sync :: { Arc , Mutex } ; pub fn create_snapshot (root_dir : & Path , rollup_lock : Arc < Mutex < crate :: RollupLock > >) -> Result < () > { println ! ("Creating snapshot of current state...") ; if ! is_git_status_clean (root_dir) ? { anyhow :: bail ! ("Git repository has uncommitted changes. Please commit or stash before creating snapshot.") ; } update_submodule_commits (root_dir , & rollup_lock) ? ; git_add_all (root_dir) ? ; let commit_message = format ! ("Update submodules and dependencies snapshot - {}" , chrono :: Utc :: now () . format ("%Y-%m-%d %H:%M:%S UTC")) ; git_commit (root_dir , & commit_message) ? ; let mut lock = rollup_lock . lock () . unwrap () ; lock . metadata . insert ("last_snapshot" . to_string () , chrono :: Utc :: now () . to_rfc3339 ()) ; drop (lock) ; println ! ("Snapshot created successfully.") ; Ok (()) }