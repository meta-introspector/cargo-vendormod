use anyhow :: { Context , Result } ; use std :: fs ; use std :: path :: { Path , PathBuf } ; use std :: process :: Command ; use toml_edit :: { DocumentMut , Value } ; pub fn normalize_url_to_flat_path (url : & str) -> PathBuf { let url = url . trim () ; let cleaned = if url . starts_with ("git@") { url . trim_start_matches ("git@") . replace (":" , "/") } else if url . starts_with ("https://") { url . trim_start_matches ("https://") . to_string () } else if url . starts_with ("http://") { url . trim_start_matches ("http://") . to_string () } else { url . to_string () } ; let path = cleaned . trim_end_matches (".git") . trim_end_matches ("/") ; PathBuf :: from (path) }