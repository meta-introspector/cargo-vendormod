use anyhow :: { Context , Result } ; use std :: path :: Path ; use std :: process :: Command ; use std :: sync :: { Arc , Mutex } ; fn git_add_all (root_dir : & Path) -> Result < () > { let output = Command :: new ("git") . arg ("add") . arg ("-A") . current_dir (root_dir) . output () . context ("Failed to execute git add") ? ; if output . status . success () { Ok (()) } else { anyhow :: bail ! ("Git add failed: {}" , String :: from_utf8_lossy (& output . stderr)) } }