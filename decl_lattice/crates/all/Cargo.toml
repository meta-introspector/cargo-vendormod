[package]
name = "all"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: all"

[dependencies]
output = { path = "../output" }
output = { path = "../output" }
output = { path = "../output" }
status = { path = "../status" }
status = { path = "../status" }
sync = { path = "../sync" }
