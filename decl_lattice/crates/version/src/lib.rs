use anyhow :: { Context , Result } ; use std :: path :: { Path , PathBuf } ; use std :: fs ; use std :: process :: Command ; use std :: collections :: HashSet ; use super :: * ; use tempfile :: tempdir ; # [doc = " Check Rust toolchain version"] fn check_rust_version (_workspace_path : & Path) -> Result < () > { println ! ("  🦀 Checking Rust toolchain version...") ; let output = Command :: new ("rustc") . arg ("--version") . output () . context ("Failed to run rustc") ? ; if output . status . success () { let version = String :: from_utf8_lossy (& output . stdout) ; println ! ("  ✅ {}" , version . trim ()) ; } Ok (()) }