[package]
name = "atlasconfig"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: AtlasConfig"

[dependencies]
crate = { path = "../crate" }
crate = { path = "../crate" }
crate = { path = "../crate" }
groupatlas = { path = "../groupatlas" }
groupatlas = { path = "../groupatlas" }
