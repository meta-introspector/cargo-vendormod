use anyhow :: { Context , Result } ; use std :: path :: { Path , PathBuf } ; use std :: fs ; use std :: process :: Command ; use std :: collections :: HashSet ; use super :: * ; use tempfile :: tempdir ; # [doc = " Check clippy lints"] fn check_clippy (workspace_path : & Path) -> Result < () > { println ! ("  🔍 Running clippy...") ; let output = Command :: new ("cargo") . args (["clippy" , "--workspace" , "--" , "-D" , "warnings"]) . current_dir (workspace_path) . output () ; match output { Ok (out) if out . status . success () => { println ! ("  ✅ Clippy check passed") ; } _ => { println ! ("  ⚠️  Clippy check found issues (consider running cargo clippy)") ; } } Ok (()) }