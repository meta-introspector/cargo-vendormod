[package]
name = "reportargs"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: ReportArgs"

[dependencies]
path = { path = "../path" }
path = { path = "../path" }
directory = { path = "../directory" }
