[package]
name = "repo"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: repo"

[dependencies]
status = { path = "../status" }
status = { path = "../status" }
repostatus = { path = "../repostatus" }
