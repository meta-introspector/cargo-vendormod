use anyhow :: { Context , Result } ; use lazy_static :: lazy_static ; use petgraph :: graph :: { Graph , NodeIndex } ; use petgraph :: visit :: Bfs ; use rayon :: prelude :: * ; use regex :: Regex ; use serde :: { Deserialize , Serialize } ; use cargo_metadata :: { MetadataCommand , DependencyKind } ; use std :: collections :: HashMap ; use std :: fs ; use std :: path :: { Path , PathBuf } ; use std :: process :: Command ; use std :: collections :: hash_map :: DefaultHasher ; use std :: hash :: { Hash , Hasher } ; fn partition_graph_by_bfs (graph : & Graph < String , () > , num_partitions : usize) -> Vec < Vec < NodeIndex > > { let mut partitions : Vec < Vec < NodeIndex > > = vec ! [Vec :: new () ; num_partitions] ; let mut visited : std :: collections :: HashSet < NodeIndex > = std :: collections :: HashSet :: new () ; for start in graph . node_indices () { if visited . contains (& start) { continue ; } let mut bfs = Bfs :: new (graph , start) ; let mut partition_id = 0 ; while let Some (node) = bfs . next (graph) { if visited . contains (& node) { continue ; } visited . insert (node) ; let target_partition = partition_id % num_partitions ; partitions [target_partition] . push (node) ; partition_id += 1 ; } } partitions }