use std :: collections :: HashMap ; use std :: fs ; use std :: process :: Command ; use std :: path :: Path ; fn test_lie_type_group_orders () -> Result < () , String > { let orders = vec ! [(2 , 6) , (3 , 168) ,] ; for (rank , expected_order) in orders { let calculated_order = calculate_lie_type_order (rank) ; assert_eq ! (calculated_order , expected_order) ; } println ! ("    ✓ Lie type group orders test passed") ; Ok (()) }