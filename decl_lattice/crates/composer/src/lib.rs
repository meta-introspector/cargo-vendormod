use std :: collections :: HashMap ; use std :: fs ; use std :: process :: Command ; use std :: path :: Path ; fn test_atlas_composer () -> Result < () , String > { let composer_name = "Atlas Composer" ; let expected_views = vec ! ["all_repos" , "Cyclic" , "Alternating" , "Lie Type" , "Sporadic"] ; assert_eq ! (composer_name , "Atlas Composer") ; assert ! (! expected_views . is_empty ()) ; println ! ("    ✓ Atlas composer test passed") ; Ok (()) }