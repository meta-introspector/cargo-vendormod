[package]
name = "selinuxpolicy"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: SELinuxPolicy"

[dependencies]
version = { path = "../version" }
version = { path = "../version" }
policy = { path = "../policy" }
