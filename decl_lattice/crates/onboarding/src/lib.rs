use anyhow :: { Context , Result } ; use std :: path :: { Path , PathBuf } ; use std :: fs ; use std :: process :: Command ; use std :: collections :: HashSet ; use super :: * ; use tempfile :: tempdir ; # [doc = " Run full onboarding workflow"] fn run_full_onboarding (workspace_path : & Path , output_dir : & Path) -> Result < () > { println ! ("\n=== Phase 1: Workspace Analysis ===") ; analyze_workspace (workspace_path , output_dir) ? ; println ! ("\n=== Phase 2: Vendoring ===") ; run_vendoring (workspace_path) ? ; println ! ("\n=== Phase 3: Upstream Sync ===") ; fetch_upstream (workspace_path) ? ; println ! ("\n=== Phase 4: Patch Generation ===") ; generate_patches (workspace_path) ? ; println ! ("\n=== Phase 5: Build Verification ===") ; verify_build (workspace_path) ? ; Ok (()) }