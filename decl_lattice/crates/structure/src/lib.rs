use anyhow :: Result ; use cargo_vendormod :: config :: Config ; use cargo_vendormod :: global_dep_graph :: { DependencyEdge , DependencyEdgeType , DependencyNode , GlobalDependencyGraph } ; use clap :: { Parser , Subcommand } ; use petgraph :: algo :: toposort as petgraph_toposort ; use petgraph :: graph :: { Graph , NodeIndex } ; use petgraph :: visit :: EdgeRef ; use std :: collections :: { HashMap , HashSet } ; use std :: fs ; use std :: path :: PathBuf ; use std :: process :: Command ; use cargo_vendormod :: global_dep_graph :: GlobalDependencyGraphBuilder ; use cargo_vendormod :: global_dep_graph :: DependencyEdgeType ; fn cmd_toml_structure (args : & GraphArgs) -> Result < () > { let input_path = args . input_path . as_ref () . ok_or_else (| | anyhow :: anyhow ! ("Input path required for toml-structure command")) ? ; println ! ("Analyzing TOML structures from {}" , input_path . display ()) ; let content = std :: fs :: read_to_string (& input_path) ? ; let graph : cargo_vendormod :: global_dep_graph :: GlobalDependencyGraph = serde_json :: from_str (& content) ? ; let mut workspace_crates = 0 ; let mut binary_crates = 0 ; let mut lib_crates = 0 ; let mut has_tests = 0 ; let mut has_benches = 0 ; for structure in & graph . toml_structures { if structure . is_workspace { workspace_crates += 1 ; } if structure . has_binaries { binary_crates += 1 ; } if ! structure . has_binaries { lib_crates += 1 ; } if structure . has_tests { has_tests += 1 ; } if structure . has_benchmarks { has_benches += 1 ; } } println ! ("\n=== TOML Structure Analysis ===") ; println ! ("Workspace crates: {}" , workspace_crates) ; println ! ("Binary crates: {}" , binary_crates) ; println ! ("Library crates: {}" , lib_crates) ; println ! ("Crates with tests: {}" , has_tests) ; println ! ("Crates with benchmarks: {}" , has_benches) ; let output_path = args . output_dir . join ("toml_structure.json") ; let report = serde_json :: json ! ({ "workspace_crates" : workspace_crates , "binary_crates" : binary_crates , "library_crates" : lib_crates , "crates_with_tests" : has_tests , "crates_with_benches" : has_benches , }) ; std :: fs :: write (& output_path , serde_json :: to_string_pretty (& report) ?) ? ; println ! ("\nTOML analysis saved to: {}" , output_path . display ()) ; Ok (()) }