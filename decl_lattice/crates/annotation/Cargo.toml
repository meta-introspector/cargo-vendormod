[package]
name = "annotation"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: Annotation"

[dependencies]
trace = { path = "../trace" }
trace = { path = "../trace" }
trace = { path = "../trace" }
trace = { path = "../trace" }
