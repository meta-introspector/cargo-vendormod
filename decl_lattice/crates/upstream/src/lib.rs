use anyhow :: { Context , Result } ; use std :: path :: { Path , PathBuf } ; use std :: fs ; use std :: process :: Command ; use std :: collections :: HashSet ; use super :: * ; use tempfile :: tempdir ; # [doc = " Fetch upstream changes"] fn fetch_upstream (workspace_path : & Path) -> Result < () > { println ! ("  🔄 Fetching upstream changes...") ; let output = Command :: new ("git") . args (["fetch" , "--all"]) . current_dir (workspace_path) . output () ; match output { Ok (out) if out . status . success () => { println ! ("  ✅ Upstream fetch complete") ; } _ => { println ! ("  ⚠️  Could not fetch upstream (may not be a git repo)") ; } } Ok (()) }