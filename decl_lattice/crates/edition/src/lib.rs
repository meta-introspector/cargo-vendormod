use anyhow :: { Context , Result } ; use std :: path :: { Path , PathBuf } ; use std :: fs ; use std :: process :: Command ; use std :: collections :: HashSet ; use super :: * ; use tempfile :: tempdir ; # [doc = " Check Rust edition"] fn check_rust_edition (workspace_path : & Path) -> Result < () > { println ! ("  🦀 Checking Rust edition...") ; let cargo_toml = workspace_path . join ("Cargo.toml") ; if ! cargo_toml . exists () { println ! ("  ⚠️  No Cargo.toml found") ; return Ok (()) ; } let content = fs :: read_to_string (& cargo_toml) ? ; for line in content . lines () { let line = line . trim () ; if line . starts_with ("edition") { println ! ("  ✅ Found edition: {}" , line) ; return Ok (()) ; } } println ! ("  ⚠️  No edition specified") ; Ok (()) }