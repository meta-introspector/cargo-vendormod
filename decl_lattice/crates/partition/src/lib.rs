use anyhow :: Result ; use cargo_vendormod :: config :: Config ; use cargo_vendormod :: global_dep_graph :: { DependencyEdge , DependencyEdgeType , DependencyNode , GlobalDependencyGraph } ; use clap :: { Parser , Subcommand } ; use petgraph :: algo :: toposort as petgraph_toposort ; use petgraph :: graph :: { Graph , NodeIndex } ; use petgraph :: visit :: EdgeRef ; use std :: collections :: { HashMap , HashSet } ; use std :: fs ; use std :: path :: PathBuf ; use std :: process :: Command ; use cargo_vendormod :: global_dep_graph :: GlobalDependencyGraphBuilder ; use cargo_vendormod :: global_dep_graph :: DependencyEdgeType ; fn cmd_partition (args : & GraphArgs) -> Result < () > { let input_path = args . input_path . as_ref () . ok_or_else (| | anyhow :: anyhow ! ("Input path required for partition command")) ? ; println ! ("Partitioning graph from {}" , input_path . display ()) ; println ! ("  Partitions: {}" , args . partition_count) ; println ! ("  Algorithm: {}" , args . algorithm) ; let content = std :: fs :: read_to_string (& input_path) ? ; let graph : cargo_vendormod :: global_dep_graph :: GlobalDependencyGraph = serde_json :: from_str (& content) ? ; let partition_size = graph . nodes . len () / args . partition_count . max (1) ; let mut partitions : Vec < Vec < String > > = (0 .. args . partition_count) . map (| _ | Vec :: new ()) . collect () ; for (i , node) in graph . nodes . iter () . enumerate () { let partition_idx = (i / partition_size) . min (args . partition_count - 1) ; partitions [partition_idx] . push (node . id . clone ()) ; } let output_path = args . output_dir . join ("partitions.json") ; let partition_data : Vec < serde_json :: Value > = partitions . iter () . enumerate () . map (| (i , nodes) | serde_json :: json ! ({ "partition_id" : i , "node_count" : nodes . len () , "nodes" : nodes })) . collect () ; std :: fs :: write (& output_path , serde_json :: to_string_pretty (& partition_data) ?) ? ; println ! ("\nPartitioning complete:") ; for (i , p) in partitions . iter () . enumerate () { println ! ("  Partition {}: {} nodes" , i , p . len ()) ; } println ! ("\nPartitions saved to: {}" , output_path . display ()) ; Ok (()) }