use anyhow :: { Context , Result } ; use clap :: Parser ; use rayon :: prelude :: * ; use std :: collections :: HashSet ; use std :: fs ; use std :: path :: { Path , PathBuf } ; use std :: process :: Command ; use std :: time :: { SystemTime , UNIX_EPOCH } ; fn extract_urls_from_table (table : & toml :: map :: Map < String , toml :: Value > , urls : & mut Vec < String >) { let git_url_re = regex :: Regex :: new (r#"https?://[a-zA-Z0-9._/-]+/([a-zA-Z0-9_-]+)/([a-zA-Z0-9._-]+)"#) . unwrap () ; for (name , value) in table { if let Some (table) = value . as_table () { if let Some (git_url) = table . get ("git") . and_then (| v | v . as_str ()) { if git_url . contains ("github.com") || git_url . contains ("gitlab") || git_url . contains ("bitbucket") { let clean_url = git_url . trim_end_matches (".git") ; if ! urls . contains (& clean_url . to_string ()) { urls . push (clean_url . to_string ()) ; } } } } if let Some (s) = value . as_str () { if s . contains ("github.com") || s . contains ("gitlab") || s . contains ("bitbucket") { for cap in git_url_re . find_iter (s) { let url = cap . as_str () . trim_end_matches (".git") . to_string () ; if ! urls . contains (& url) { urls . push (url) ; } } } } } }