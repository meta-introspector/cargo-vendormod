[package]
name = "invariant"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: Invariant"

[dependencies]
id = { path = "../id" }
config = { path = "../config" }
config = { path = "../config" }
graph = { path = "../graph" }
dot = { path = "../dot" }
