[package]
name = "sync"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: sync"

[dependencies]
config = { path = "../config" }
config = { path = "../config" }
config = { path = "../config" }
