use anyhow :: { Context , Result } ; use clap :: { Parser , ArgAction } ; use std :: collections :: HashMap ; use std :: fs ; use std :: path :: { Path , PathBuf } ; use std :: process :: Command ; use toml_edit :: { Document , DocumentMut , Value } ; use glob :: glob ; use lazy_static :: lazy_static ; use regex :: Regex ; use super :: * ; use std :: path :: PathBuf ; use tempfile :: tempdir ; impl AppContext { pub fn from_args (args : & crate :: args :: Args) -> Result < Self > { let compile_time_manifest_dir = PathBuf :: from (env ! ("CARGO_MANIFEST_DIR")) ; let vendormod_cargo_toml_path = compile_time_manifest_dir . join ("Cargo.toml") ; let vendormod_cargo_toml_doc : Option < DocumentMut > = if vendormod_cargo_toml_path . exists () { match fs :: read_to_string (& vendormod_cargo_toml_path) { Ok (content) => content . parse :: < DocumentMut > () . ok () , Err (_) => None , } } else { None } ; let git_executable_path = if let Some (ref doc) = vendormod_cargo_toml_doc { doc . get ("package") . and_then (| item | item . as_table ()) . and_then (| table | table . get ("metadata")) . and_then (| item | item . as_table ()) . and_then (| table | table . get ("repo-manager")) . and_then (| item | item . as_table ()) . and_then (| table | table . get ("git_path")) . and_then (| item | item . as_str ()) . map (PathBuf :: from) . unwrap_or_else (| | PathBuf :: from ("git")) } else { PathBuf :: from ("git") } ; let vendormod_config = vendormod_cargo_toml_doc . as_ref () . and_then (| doc | { doc . get ("package") . and_then (| item | item . as_table ()) . and_then (| table | table . get ("metadata")) . and_then (| item | item . as_table ()) . and_then (| table | table . get ("cargo-vendormod")) . and_then (| item | item . as_table ()) }) ; let root_dir = args . root_dir . canonicalize () . context ("Failed to canonicalize root_dir") ? ; let mut manifest_path = if let Some (ref p) = args . manifest_path { p . clone () } else { let standard = root_dir . join ("Cargo.toml") ; if standard . exists () { standard } else { let pattern = format ! ("{}/{}Cargo.toml" , root_dir . display () , if root_dir . as_os_str () . to_string_lossy () . ends_with ('/') { "" } else { "*" }) ; match glob :: glob (& pattern) { Ok (entries) => entries . filter_map (| e | e . ok ()) . find (| p | { p . file_name () . and_then (| n | n . to_str ()) . map (| s | ! s . starts_with ('.') && ! s . starts_with ('#')) . unwrap_or (false) }) . unwrap_or (standard) , Err (_) => standard , } } } ; let temp_symlink = if manifest_path . file_name () != Some (std :: ffi :: OsStr :: new ("Cargo.toml")) { let link = root_dir . join ("Cargo.toml") ; if ! link . exists () { std :: os :: unix :: fs :: symlink (& manifest_path , & link) . context ("Failed to create symlink for manifest") ? ; Some (link . clone ()) } else { None } } else { None } ; if temp_symlink . is_some () { manifest_path = root_dir . join ("Cargo.toml") ; } let get_str = | table : Option < & toml_edit :: Table > , key : & str | -> Option < String > { table . and_then (| t | t . get (key)) . and_then (| v | v . as_str ()) . map (| s | s . to_string ()) } ; let get_path = | table : Option < & toml_edit :: Table > , key : & str | -> Option < PathBuf > { get_str (table , key) . map (PathBuf :: from) } ; let get_bool = | table : Option < & toml_edit :: Table > , key : & str | -> Option < bool > { table . and_then (| t | t . get (key)) . and_then (| v | v . as_bool ()) } ; let submodules_dir = if let Some (ref p) = args . submodules_path { if p . is_absolute () { p . clone () } else { root_dir . join (p) } } else if let Some (path_str) = get_str (vendormod_config , "submodules-dir") { let p = PathBuf :: from (path_str) ; if p . is_absolute () { p } else { root_dir . join (p) } } else { root_dir . join (DEFAULT_SUBMODULES_DIR) } ; let mirrors_path = if let Some (ref p) = args . mirrors_path { if p . is_absolute () { p . clone () } else { root_dir . join (p) } } else if let Some (path_str) = get_str (vendormod_config , "bare-mirrors-dir") { let p = PathBuf :: from (path_str) ; if p . is_absolute () { p } else { root_dir . join (p) } } else { PathBuf :: from (DEFAULT_MIRRORS_PATH) } ; let vendor_dir = if let Some (ref p) = args . vendor_dir { if p . is_absolute () { p . clone () } else { root_dir . join (p) } } else if let Some (path_str) = get_str (vendormod_config , "vendor-dir") { let p = PathBuf :: from (path_str) ; if p . is_absolute () { p } else { root_dir . join (p) } } else { root_dir . join (DEFAULT_VENDOR_DIR) } ; let target_branch = args . target_branch . clone () . or_else (| | { get_str (vendormod_config , "default-target-branch") . or_else (| | Some (DEFAULT_TARGET_BRANCH . to_string ())) }) . unwrap_or_else (| | DEFAULT_TARGET_BRANCH . to_string ()) ; let version_branch_format = args . version_branch_format . clone () . or_else (| | { get_str (vendormod_config , "version-branch-format") . or_else (| | Some (DEFAULT_VERSION_BRANCH_FORMAT . to_string ())) }) . unwrap_or_else (| | DEFAULT_VERSION_BRANCH_FORMAT . to_string ()) ; let create_version_branches = args . create_version_branches . unwrap_or_else (| | { get_bool (vendormod_config , "create-version-branches") . unwrap_or (DEFAULT_CREATE_VERSION_BRANCHES) }) ; Ok (AppContext { git_executable_path , root_dir , manifest_path , submodules_dir , mirrors_path , vendor_dir , target_branch , create_version_branches , version_branch_format , dry_run : args . dry_run , verbose : args . verbose , temp_symlink , }) } }