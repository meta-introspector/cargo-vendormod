[package]
name = "labels"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: LABELS"

[dependencies]
annotations = { path = "../annotations" }
trace = { path = "../trace" }
trace = { path = "../trace" }
trace = { path = "../trace" }
trace = { path = "../trace" }
