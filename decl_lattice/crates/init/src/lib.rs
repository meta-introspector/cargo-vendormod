use anyhow :: { Context , Result } ; use cargo_vendormod :: config :: Config ; use cargo_vendormod :: args :: Args ; use clap :: { Parser , Subcommand } ; use std :: path :: PathBuf ; use std :: process :: Command ; use rayon :: prelude :: * ; use crossbeam :: channel ; use cargo_vendormod :: submodule_discovery :: { discover_submodules , clone_submodules_to_target } ; fn cmd_init (args : & VendoringArgs , config : & Config , root_dir : & PathBuf) -> Result < () > { println ! ("=== Initializing vendoring ===") ; let submodules_path = args . submodules_path . clone () . unwrap_or_else (| | root_dir . join (& config . submodules_dir)) ; let mirrors_path = args . mirrors_path . clone () . unwrap_or_else (| | config . mirrors_dir . clone ()) ; println ! ("Submodules: {}" , submodules_path . display ()) ; println ! ("Mirrors: {}" , mirrors_path . display ()) ; if ! args . dry_run { std :: fs :: create_dir_all (& submodules_path) ? ; std :: fs :: create_dir_all (& mirrors_path) ? ; } use cargo_vendormod :: submodule_discovery :: { discover_submodules , clone_submodules_to_target } ; println ! ("Discovering submodules from {}" , args . source_repo . display ()) ; let submodules = cargo_vendormod :: submodule_discovery :: discover_submodules (& args . source_repo) ? ; println ! ("Found {} submodules" , submodules . len ()) ; for sm in & submodules { println ! ("  - {} -> {}" , sm . path . display () , sm . url) ; } if ! args . dry_run { let git_exe = cargo_vendormod :: submodule_discovery :: get_git_executable () ; cargo_vendormod :: submodule_discovery :: clone_submodules_to_target (submodules , & submodules_path , & git_exe) ? ; } println ! ("=== Vendoring initialized ===") ; Ok (()) }