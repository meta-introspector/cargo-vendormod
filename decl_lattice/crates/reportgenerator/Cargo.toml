[package]
name = "reportgenerator"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: ReportGenerator"

[dependencies]
n = { path = "../n" }
format = { path = "../format" }
output = { path = "../output" }
output = { path = "../output" }
output = { path = "../output" }
