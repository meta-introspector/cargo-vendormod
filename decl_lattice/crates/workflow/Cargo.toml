[package]
name = "workflow"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: workflow"

[dependencies]
onboarding = { path = "../onboarding" }
onboarding = { path = "../onboarding" }
onboarding = { path = "../onboarding" }
onboarding = { path = "../onboarding" }
