[package]
name = "forks"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: forks"

[dependencies]
output = { path = "../output" }
output = { path = "../output" }
output = { path = "../output" }
status = { path = "../status" }
status = { path = "../status" }
version = { path = "../version" }
version = { path = "../version" }
