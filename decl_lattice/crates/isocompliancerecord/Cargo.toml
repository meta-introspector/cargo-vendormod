[package]
name = "isocompliancerecord"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: IsoComplianceRecord"

[dependencies]
status = { path = "../status" }
status = { path = "../status" }
