[package]
name = "action"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: action"

[dependencies]
crate = { path = "../crate" }
crate = { path = "../crate" }
crate = { path = "../crate" }
