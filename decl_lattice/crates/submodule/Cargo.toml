[package]
name = "submodule"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: submodule"

[dependencies]
output = { path = "../output" }
output = { path = "../output" }
output = { path = "../output" }
