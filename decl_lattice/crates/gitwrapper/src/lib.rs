use anyhow :: Result ; use std :: path :: { Path , PathBuf } ; use std :: process :: Command ; use super :: * ; use std :: path :: PathBuf ; impl GitWrapper { # [doc = " Create a new GitWrapper"] pub fn new (git_exe : PathBuf) -> Self { Self { git_exe } } # [doc = " Get the git executable path"] pub fn git_exe (& self) -> & PathBuf { & self . git_exe } # [doc = " Clone a repository"] pub fn clone (& self , url : & str , target_dir : & Path) -> Result < () > { self . direct_git_clone (url , target_dir) } # [doc = " Checkout a branch or commit"] pub fn checkout (& self , repo_path : & Path , refspec : & str) -> Result < () > { self . direct_git_checkout (repo_path , refspec) } # [doc = " Get list of branches"] pub fn get_branches (& self , repo_path : & Path) -> Result < Vec < String > > { self . direct_git_get_branches (repo_path) } # [doc = " Get list of tags"] pub fn get_tags (& self , repo_path : & Path) -> Result < Vec < String > > { self . direct_git_get_tags (repo_path) } # [doc = " Check if a branch exists"] pub fn branch_exists (& self , repo_path : & Path , branch_name : & str) -> Result < bool > { self . direct_git_branch_exists (repo_path , branch_name) } fn direct_git_clone (& self , url : & str , target_dir : & Path) -> Result < () > { let output = Command :: new (& self . git_exe) . arg ("clone") . arg (url) . arg (target_dir) . output () ? ; if ! output . status . success () { let error_msg = String :: from_utf8_lossy (& output . stderr) ; return Err (anyhow :: anyhow ! ("Git clone failed: {}" , error_msg)) ; } Ok (()) } fn direct_git_checkout (& self , repo_path : & Path , refspec : & str) -> Result < () > { let output = Command :: new (& self . git_exe) . arg ("-C") . arg (repo_path) . arg ("checkout") . arg (refspec) . output () ? ; if ! output . status . success () { let error_msg = String :: from_utf8_lossy (& output . stderr) ; return Err (anyhow :: anyhow ! ("Git checkout failed: {}" , error_msg)) ; } Ok (()) } fn direct_git_get_branches (& self , repo_path : & Path) -> Result < Vec < String > > { let output = Command :: new (& self . git_exe) . arg ("-C") . arg (repo_path) . arg ("branch") . arg ("--list") . output () ? ; if ! output . status . success () { let error_msg = String :: from_utf8_lossy (& output . stderr) ; return Err (anyhow :: anyhow ! ("Git branch list failed: {}" , error_msg)) ; } let branches = String :: from_utf8_lossy (& output . stdout) ; Ok (branches . lines () . map (| line | line . trim () . trim_start_matches ("* ") . to_string ()) . collect ()) } fn direct_git_get_tags (& self , repo_path : & Path) -> Result < Vec < String > > { let output = Command :: new (& self . git_exe) . arg ("-C") . arg (repo_path) . arg ("tag") . arg ("--list") . output () ? ; if ! output . status . success () { let error_msg = String :: from_utf8_lossy (& output . stderr) ; return Err (anyhow :: anyhow ! ("Git tag list failed: {}" , error_msg)) ; } let tags = String :: from_utf8_lossy (& output . stdout) ; Ok (tags . lines () . map (| line | line . trim () . to_string ()) . collect ()) } fn direct_git_branch_exists (& self , repo_path : & Path , branch_name : & str) -> Result < bool > { let output = Command :: new (& self . git_exe) . arg ("-C") . arg (repo_path) . arg ("show-ref") . arg ("--verify") . arg (format ! ("refs/heads/{}" , branch_name)) . output () ? ; Ok (output . status . success ()) } }