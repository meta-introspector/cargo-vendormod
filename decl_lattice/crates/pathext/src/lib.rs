use anyhow :: { Context , Result } ; use clap :: Parser ; use rayon :: prelude :: * ; use std :: collections :: HashSet ; use std :: fs ; use std :: path :: { Path , PathBuf } ; use std :: process :: Command ; impl PathExt for PathBuf { fn expand_user (self) -> Result < PathBuf > { if let Some (s) = self . to_str () { if s . starts_with ("~") { let home = dirs :: home_dir () . context ("No home directory") ? ; return Ok (home . join (s . trim_start_matches ("~") . trim_start_matches ('/'))) ; } } Ok (self) } }