[package]
name = "dataflownodetype"
version = "0.1.0"
edition = "2021"
description = "Decl lattice node: DataFlowNodeType"

[dependencies]
config = { path = "../config" }
config = { path = "../config" }
graph = { path = "../graph" }
dot = { path = "../dot" }
