use std :: fs ; use std :: path :: Path ; use clap :: Parser ; use serde_json ; use num_bigint :: BigUint ; use cargo_vendormod :: group_atlas :: { GroupAtlas , GroupFamily , ClassicalLieType , TwistedLieType , SporadicGroup , n } ; use cargo_vendormod :: visualization :: { AtlasRenderer , AtlasConfig } ; # [doc = " Generate statistics output"] fn generate_statistics (renderer : & AtlasRenderer , atlas : & GroupAtlas , output_dir : & str) -> Result < () , Box < dyn std :: error :: Error > > { let stats = renderer . export_atlas_data (atlas) ; let stats_path = Path :: new (output_dir) . join ("statistics.json") ; fs :: write (& stats_path , stats) ? ; println ! ("📈 Statistics saved to: {}" , stats_path . display ()) ; print_summary_stats (atlas) ; Ok (()) }