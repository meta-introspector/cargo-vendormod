# Skills

Agent skills for this checkout, grouped by platform.

## Kilo / cargo-vendormod

- `cargo-vendormod-whitespace-access` — sanitize/normalize whitespace and untrusted input
- `cargo-vendormod-atlas` — finite simple group atlas + ZKperf coverage
- `cargo-vendormod-graph` — dependency graph build/analyze/partition/visualize
- `cargo-vendormod-processing` — layered crate processing + Nix flakes
- `cargo-vendormod-reporting` — report generation, gitmodules metadata, coverage
- `cargo-vendormod-tests` — test runner and unit test guidance
- `cargo-vendormod-troubleshooting` — diagnostics and recovery
- `cargo-vendormod-vendoring` — git submodule vendoring workflow
- `cargo-vendormod-workload` — recursive workload processing and reporting
- `cargo-vendormod-scanner` — repo scan and Cargo.toml discovery
- `cargo-vendormod-multi-lang` — Java/JavaScript/C/Lean4 detection + flakes
- `dag-cbor-restriction-morphisms` — CBOR/DAG-CBOR/DRISL/libipld/serde_ipld_dagcbor workflows

## dotagents / OpenCode / Claude

- `dotagents-providers-hermes` — Hermes provider deploy/config
- `dotagents-providers-gemini` — Gemini provider deploy/config
- `dotagents-providers-pi` — Pi provider deploy/config

## Generated assets

- `skill-exports/` — auto-generated cross-agent manifests from `src/bin/skill-importer.rs`
