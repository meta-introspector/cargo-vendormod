# THIS FILE IS AUTO-GENERATED by cargo-vendormod
# DO NOT EDIT MANUALLY — regenerate with:
#   cargo-vendormod process crates --generate-flakes
#
# Crate: test_n0_dasl
# Version: 0.1.0

{
  description = "test_n0_dasl - 0.1.0";

  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixpkgs-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };

  outputs =
    { self, nixpkgs, flake-utils }:
    flake-utils.lib.eachDefaultSystem (
      system:
      let
        pkgs = import nixpkgs { inherit system; };
        real_src = ./.;
      in
      {
        packages = {
          default = pkgs.callPackage ./package.nix { src = real_src; };
          test_n0_dasl = self.packages.${system}.default;
        };

        devShells.default = pkgs.mkShell {
          buildInputs = with pkgs; [
            rustc
            cargo
            clippy
            rustfmt
          ];
        };

        # Verification: nix flake check
        checks.default = self.packages.${system}.default;
      }
    );
}
